# Jersey Texture Prototypes

Prototype SVG jersey textures for the AR team customization flow.

These are intentionally lightweight test assets. They provide stable file names
for all 12 scanner targets before the final UV-specific jersey artwork exists.

The current model supports jersey material color changes. Texture placement will
be validated in the UV phase before these assets are connected to AR runtime.
